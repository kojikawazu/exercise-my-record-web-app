# API設計（案）

## 前提
- データソースはAPI先行設計
- 1日1レコードの上書きはサーバ側で実施（POSTでUpsert）
- 取得系エンドポイントは `/records`（一覧）と `/records/:date`（詳細）
- Supabase を使用予定（このリポジトリでは設定/実装しない）
- 認証/認可は書き込み系のみ必須（POST/PATCH/DELETE）
- プロフィール（体重）はSupabaseに保存する

## エンドポイント
### POST /records
- 用途: 1日1レコードの作成
- 挙動: 同日が存在する場合はエラーを返す（上書きしない）

### GET /records
- 用途: 一覧取得
- 並び順: 日付の新しい順（降順）
- クエリ: `page=N`（1始まり、limit=10 固定）
- `page` 未指定/NaN/0以下 → 1 に正規化
- `page > totalPages` → 最終ページに clamp
- レスポンス形式:
  ```json
  {
    "records": [{ date, totalSets, cardioMinutes, cardioDistance, cardios }],
    "totalCount": 25,
    "page": 1,
    "totalPages": 3
  }
  ```

### GET /records/:date
- 用途: 詳細取得
- 返却項目（最小）:
  - date
  - memo
  - workouts (part/name/sets/reps/weight)
  - cardios (type/minutes/distance) ※複数行

### GET /records/calendar?month=YYYY-MM
- 用途: カレンダー用の月別記録有無取得
- 返却項目: 記録がある日付の配列

### GET /records/trends?period=1w|1m|3m|all
- 用途: 推移グラフ用データ取得
- 返却項目:
  - dates (日付配列)
  - weight (体重配列)
  - totalSets (セット数配列)
  - cardioDistance (距離配列)
  - calories (カロリー配列)

## 管理者専用API
### 範囲
- 削除 / 編集 / マスター管理 / エクスポート（Issue #20 で削除予定）

### GET /admin/export（Issue #20 で削除予定、現時点では現役）
- 用途: データ出力（管理者のみ）
- クエリ: `from=YYYY-MM-DD`, `to=YYYY-MM-DD`, `format=csv|json`
- 実装: `/api/admin/export`

### DELETE /records/:date
- 用途: レコード削除（管理者のみ）

### PATCH /records/:date
- 用途: レコード編集（管理者のみ）

### マスター管理
- `GET /masters?type=body-parts|exercises|cardio-types`
- `POST /masters?type=...`
- `PATCH /masters/:id`
- `DELETE /masters/:id`

### プロフィール
- `GET /profile`（体重取得）
- `POST /profile`（体重保存）
- 暫定: `/api/profile` はフロントの仮実装で使用（本番はSupabase想定）

## 認証/認可
- 読み取り系（GET）は認証不要（一般ユーザーが閲覧可能）
- 書き込み系（POST/PATCH/DELETE）は管理者認証が必要
  - リクエストヘッダーに `Authorization: Bearer <access_token>` を付与
  - サーバー側で Supabase `auth.getUser()` によるトークン検証 + `ADMIN_EMAIL` 一致チェック
  - 認証なし → 401、管理者以外 → 403
- 共通認証ヘルパー: `front/src/lib/adminAuth.ts` の `requireAdmin(request)`
- フロント側: `front/src/lib/authFetch.ts` の `authFetch()` で Supabase セッショントークンを自動付与
- E2E テスト: `NODE_ENV !== 'production'` かつ `NEXT_PUBLIC_E2E_BYPASS=1` の場合は認証をバイパス

### 認証が必要なエンドポイント
| エンドポイント | メソッド |
|---------------|---------|
| `/api/records` | POST |
| `/api/records/:date` | PATCH, DELETE |
| `/api/masters` | POST |
| `/api/masters/:id` | PATCH, DELETE |
| `/api/profile` | POST |
| `/api/admin/export` | GET |

## バリデーション方針
- フロント側: フィールド単位でエラーを表示し、エラーがある場合は保存を抑止する（Issue #19 で実装）
- サーバ側: 別issueで対応予定。現時点ではフロントバリデーションのみ
## 更新
- 2026-02-03: 認証はSupabaseのGoogle OAuth実装を前提に整理
- 2026-02-03: 管理者ページは未ログイン時に /admin/login へ遷移
- 2026-02-03: E2EはPlaywright起動時のみテストログインを有効化
- 2026-02-03: PrismaはDATABASE_URL未設定時にフォールバック
- 2026-03-21: エクスポートAPIは Issue #20 で削除予定（現時点では現役維持）、カレンダー/推移API追加、一覧ページング対応、有酸素複数行対応、バリデーション方針確定
